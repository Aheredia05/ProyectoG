{% extends 'base.html.twig' %}

{% block title %}Base1{% endblock %}

{% block body %}
    <h1>Base1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ base1.id }}</td>
            </tr>
            <tr>
                <th>Idbase1</th>
                <td>{{ base1.idbase1 }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ base1.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ base1.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('base1_index') }}">back to list</a>

    <a href="{{ path('base1_edit', {'id': base1.id}) }}">edit</a>

    {{ include('base1/_delete_form.html.twig') }}
{% endblock %}
